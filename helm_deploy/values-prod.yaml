---
# Per environment values which override defaults in hmpps-parom-service-api/values.yaml

generic-service:
  ingress:
    host: parom-service-api-prod.hmpps.service.justice.gov.uk
    # modsecurity_enabled: true

  env:
    HMPPS_AUTH_URL: "https://sign-in.hmpps.service.justice.gov.uk/auth"
    NDELIUS-INTEGRATION-API_URL: "https://parom-and-delius.hmpps.service.justice.gov.uk"
    FRONTEND_URL: "https://parom-service-ui-prod.hmpps.service.justice.gov.uk"
 
  allowlist:

    groups:
      - digital_staff_and_mojo
      - unilink_staff
      - moj_cloud_platform
      - delius-prod

# CloudPlatform AlertManager receiver to route prometheus alerts to slack
# See https://user-guide.cloud-platform.service.justice.gov.uk/documentation/monitoring-an-app/how-to-create-alarms.html#creating-your-own-custom-alerts
generic-prometheus-alerts:
  alertSeverity: hmpps-parom-service-notifications
